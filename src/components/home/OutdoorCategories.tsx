import SectionContainer from "../common/SectionContainer"

type Props = {}

const OutdoorCategories = (props: Props) => {
    return (
        <SectionContainer
            sectionClass="mb-section"
            className="bg-white border border-gray-300 rounded-card"
        >
            OutdoorCategories
        </SectionContainer>
    )
}

export default OutdoorCategories