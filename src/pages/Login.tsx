import SectionContainer from "../components/common/SectionContainer"

type Props = {}

const Login = (props: Props) => {
  return (
    <SectionContainer className={"p-section bg-white mt-section mb-section-30 border border-gray-300 rounded"}>
      login
    </SectionContainer>
  )
}

export default Login