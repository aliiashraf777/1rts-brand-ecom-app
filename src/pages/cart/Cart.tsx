import SectionContainer from "../../components/common/section/SectionContainer"

type Props = {}

const Cart = (props: Props) => {
  return (
    <SectionContainer>
      Cart Page
    </SectionContainer>
  )
}

export default Cart