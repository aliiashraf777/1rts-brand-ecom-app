import SectionContainer from "../../components/common/SectionContainer"

type Props = {}

const Cart = (props: Props) => {
  return (
    <SectionContainer>
      Cart Page
    </SectionContainer>
  )
}

export default Cart