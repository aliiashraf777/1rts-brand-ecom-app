import Hero from "../components/home/Hero"

type Props = {}

const Home = (props: Props) => {
    return (
        <div className="w-full">
            <Hero />
        </div>
    )
}

export default Home